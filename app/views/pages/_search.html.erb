<%= form_tag tanks_path, method: :get do %>
  <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "Find a tank"
  %>
  <%= submit_tag "Search", class: "btn btn-primary" %>
<% end %>

<%#= simple_form_for :search, url: tank_path, method: "GET", html: { class: 'form-inline' } do |f| %>
  <%#= f.input :name, input_html: {value: ""} %>
  <%#= f.submit "Search", class: "btn btn-primary" %>
<%# end %>
